import 'package:flutter/material.dart';
import 'package:ionicons/ionicons.dart';

class FilterBatchesButtonWidget extends StatelessWidget {
  const FilterBatchesButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: const Icon(Ionicons.filter));
  }
}
